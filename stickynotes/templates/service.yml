kind: Service
apiVersion: v1
metadata:
  name: stickynotes-service
spec:
  selector:
    app: stickynotes-container
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000
  type: LoadBalancer
