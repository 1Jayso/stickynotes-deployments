



apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  labels:
    app: postgres-container
spec:
  ports:
  - port: 5432
    name: postgresql-port
  clusterIP: None
  selector:
      app: postgres-container



# apiVersion: v1
# kind: Service
# metadata:
#   name: postgres-service
# spec:
#   selector:
#     app: postgres-container
#   ports:
#       port: 5432
#       targetPort: 5432
#   type: ClusterIP
